version: 1

sphinx:
  configuration: docs/conf.py

formats: []

python:
  version: 3.7
  install:
    - requirements: requirements.txt
    - method: pip
      path: .
      extra_requirements:
        - docs
    - method: setuptools
      path: bcs
  system_packages: true
